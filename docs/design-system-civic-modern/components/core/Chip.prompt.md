Pill that names a real product action; float at most two over a photo, or use `tone="status"` inside a tracker card.

```jsx
<Chip dot="sage">Benefits check</Chip>
<Chip tone="status" dot="sage">Interviewing</Chip>
```

Never decorative. If the product can't do it, it can't be a chip.
