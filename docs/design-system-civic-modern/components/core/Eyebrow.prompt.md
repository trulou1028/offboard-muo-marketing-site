Uppercase label above a section headline; every section gets one.

```jsx
<Eyebrow tone="forest">Career context</Eyebrow>
```

Tone follows the band: Sand bands use `sand`, dark AI bands use `lumo`, other dark bands use `onDark`.
