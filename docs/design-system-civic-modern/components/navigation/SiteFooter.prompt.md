Bottom of every marketing page; sits on Forest deep, which is why the final CTA band above it must not also be dark unless they read as one anchor.

```jsx
<SiteFooter logo="/assets/logo-wordmark-light.png" columns={[{title:'Product',items:['How it works','Pricing']}]} note="© 2026 Offboard" />
```
