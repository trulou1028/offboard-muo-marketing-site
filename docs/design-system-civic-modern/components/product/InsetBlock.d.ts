/** Forest #16351F inset block on a Paper band. One per page; the sponsor/B2B hero device. */
export interface InsetBlockProps {
  children?: React.ReactNode;
  style?: React.CSSProperties;
  contentStyle?: React.CSSProperties;
}
export declare function InsetBlock(props: InsetBlockProps): JSX.Element;
