# Copy to .env.local and fill in. The site currently makes zero backend calls
# (a test enforces it) — these are for upcoming needs like waitlist forms.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
SUPABASE_PROJECT_REF=
SUPABASE_DB_PASSWORD=
